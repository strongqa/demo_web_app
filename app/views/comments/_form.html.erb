<%= form_for([@article, @article.comments.build]) do |f| %>
      <%= f.text_area :body, class: 'form-control form__textarea' %>
      <%= f.submit('Create comment', class: 'btn btn-success btn__form') %>
<% end %>
