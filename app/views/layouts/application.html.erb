<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? "#{ApplicationHelper::APPLICATION_NAME} - #{yield(:title)}" : ApplicationHelper::APPLICATION_NAME %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Demo Www" %>">
  <link href='http://fonts.googleapis.com/css?family=Montserrat:400,700%7CPT+Serif:400,700,400italic' rel='stylesheet'>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header>
  <%= render 'layouts/navigation' %>
</header>
<main role="main">
  <%= render 'layouts/messages' %>
  <section class="hero"
           style="background-image: url(http://deothemes.com/envato/enigma/html/img/blog/blog_title_bg.jpg)">
            <!-- TODO replace background-image url to proper from cdn-->
    <div class="hero__content">
      <h1 class="hero__heading"><%= yield(:title) %></h1>
      <%= render 'layouts/breadcrumb' %>
    </div>
  </section>
  <aside id="sidebar">
    <section id="banner"></section>
  </aside>
  <div class="container">
    <div class="row">
      <%= content_for?(:content) ? yield(:content) : yield %>
    </div>
  </div>

</main>

<footer class="footer">
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <p>Copyright StrongQA Ltd.</p>
      </div>
    </div>
  </div>
</footer>

</body>
</html>
